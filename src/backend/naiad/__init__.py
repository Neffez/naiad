# Dev placeholder. The real version is stamped from the git tag by the release
# workflow (.github/workflows/release.yml) when the image is built, so this never
# needs a manual bump — just tag `vX.Y.Z`. Non-release/local builds report this.
__version__ = "0.0.0.dev0"
